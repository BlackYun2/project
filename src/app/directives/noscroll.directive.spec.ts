import { NoscrollDirective } from './noscroll.directive';

describe('NoscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NoscrollDirective();
    expect(directive).toBeTruthy();
  });
});
